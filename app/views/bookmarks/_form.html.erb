<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment, prompt: "Must be more than 6 characters" %>
  <%= f.association :movie %>
  <%= f.button :submit, class: "btn btn-primary mt-3" %>
<% end %>
